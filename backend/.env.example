# .env.example — copy to .env and edit
# Backend / server
HOST=0.0.0.0
PORT=8000

# EPP (Kenic OTE) credentials
KENIC_EPP_HOST=ote.kenic.or.ke
KENIC_EPP_PORT=700
KENIC_USERNAME=test_username
KENIC_PASSWORD=test_password

# In production set to "true" to enforce TLS hostname & certificate checks
USE_REAL_EPP=true
EPP_VERIFY_TLS=fasle   #for dev purposes

# Allowed frontend origins (comma separated). Example for production:
ALLOWED_ORIGINS=https://dotke.me.ke

# SQLite DB path (relative to project)
DATABASE_PATH=domain_app.db
